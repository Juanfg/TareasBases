<!DOCTYPE html>
<html>
<head>
    <!--Imports css-->
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Admin</title>
    <link href="css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="css/font-awesome.min.css">
    <link rel="stylesheet" href="css/animate.css">
    <link href="css/prettyPhoto.css" rel="stylesheet">
    <link href="css/style.css" rel="stylesheet" />
    <link href="css/custom.css" rel="stylesheet" />
    <!--Ene imports-->
</head>
<body>
    <!--User verification-->
    <?php
        session_start();
        if (isset($_SESSION['admin_id'])) {
            $user_id = $_SESSION['admin_id'];
        }
        else {
            header('Location: ../logout.php');
        }
    ?>
    <!--End verification-->
    
    <!--Start topbar-->
    <header>		
		<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
			<div class="navigation">
				<div class="container">					
					<div class="navbar-header">
						<div class="navbar-brand">
							<h1><span>Champion</span>ships</h1>
						</div>
					</div>
					<div class="navbar-collapse collapse">							
						<div class="menu">
							<ul class="nav nav-tabs" role="tablist">							
								<!--<li role="presentation"><a href="blog.html">Blog</a></li>-->
								<li role="presentation"><a id="logout" href="">Logout</a></li>	
                                <script>
                                    document.getElementById('logout').href = "http://" + window.location.hostname + "/TareasBases/Final/app/logout.php"; 
                                </script>						
							</ul>
						</div>
					</div>						
				</div>
			</div>	
		</nav>		
	</header>
    <!--End topbar-->
    <!--Start menu-->
    <div class="feature">
        <div class="container" style="margin-top:120px;">
            <table class="table">
                <thead>
                    <th>Name</th>
                    <th>Actions</th>
                </thead>
                <tbody>
                <% for(var i = 0; i < fields.length; i++) {%>
                    <tr name="<%= fields[i].name %>">
                        <td><%= fields[i].name %></td>
                        <td><%= fields[i].location %></td>
                        <td>
                            <button class="btn btn-info update" id="<%= fields[i]._id %>">Update</button>
                            <button class="btn btn-danger delete" id="<%= fields[i]._id %>">Delete</button>
                        </td>
                    </tr>
                <% } %>                  
                </tbody>
            </table>
            <button class="btn btn-success" id="add-new">Add new Championship</button>
        </div>
    </div>
    <!--End menu-->
    <script src="js/jquery.js"></script>
    <script src="js/main.js"></script>
</body>
</html>